import { Component, computed, signal } from '@angular/core';
import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { HttpClient, HttpHeaders } from '@angular/common/http';

type Field={id:string;type:string;label:string;required?:boolean;options?:{id:string;label:string}[];visibleWhen?:{fieldId:string;equals:string}}; type FormDef={contractVersion:string;formKey:string;title:string;pages:{id:string;title:string;fields:Field[]}[]};
@Component({selector:'app-root',standalone:true,imports:[CommonModule,FormsModule],template:`
<div class="min-h-screen">
<header class="border-b border-stone-200 bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><div><p class="type-caption-bold text-emerald-700">SMART INTAKE</p><h1 class="type-h3 m-0">Form Builder Lite</h1></div><div class="flex items-center gap-3"><span class="rounded-full bg-stone-100 px-3 py-1 type-caption">Workspace · Northstar</span><button class="rounded-full border border-emerald-700 px-4 py-2 type-label text-emerald-800" (click)="view.set('admin')">Staff admin</button></div></div></header>
<main class="mx-auto max-w-7xl px-5 py-7">
<nav class="mb-6 flex flex-wrap gap-2" aria-label="Main navigation"><button *ngFor="let n of nav" (click)="view.set(n.id)" class="rounded-full px-4 py-2 type-label" [class.bg-emerald-700]="view()===n.id" [class.text-white]="view()===n.id" [class.bg-white]="view()!==n.id">{{n.label}}</button></nav>
<section *ngIf="view()==='forms'" class="space-y-6"><div class="rounded-3xl bg-emerald-800 p-7 text-white"><p class="type-label">CANONICAL, ACCESSIBLE, ADAPTIVE</p><h2 class="type-h2 mt-2">Build intake experiences people can finish with confidence.</h2><p class="max-w-2xl type-body">Create a versioned definition, test real rules in preview, then publish an immutable release. Respondents can pause, return, validate, and receive a receipt.</p><button (click)="view.set('builder')" class="mt-3 rounded-full bg-white px-5 py-3 type-label text-emerald-800">Create a form</button></div><div class="rounded-3xl bg-white p-6 shadow-sm"><div class="mb-5 flex items-center justify-between"><div><h2 class="type-h4">Forms</h2><p class="type-body text-stone-600">Catalog identity and release status stay separate from drafts.</p></div><button (click)="loadForms()" class="rounded-full border border-stone-300 px-4 py-2 type-label">Refresh</button></div><div *ngIf="forms().length;else empty" class="grid gap-3"><button *ngFor="let f of forms()" (click)="open(f)" class="flex items-center justify-between rounded-2xl border border-stone-200 p-4 text-left hover:border-emerald-500"><div><p class="type-label">{{f.title}}</p><p class="type-caption text-stone-600">{{f.formKey}} · revision {{f.revision}}</p></div><span class="rounded-full bg-emerald-50 px-3 py-1 type-caption text-emerald-800">{{f.status}}</span></button></div><ng-template #empty><div class="rounded-2xl border border-dashed border-stone-300 p-8 text-center"><p class="type-label">No forms yet</p><p class="type-body text-stone-600">Start with the included conditional intake template.</p></div></ng-template></div></section>
<section *ngIf="view()==='builder'" class="grid gap-5 lg:grid-cols-[15rem_1fr_18rem]"><aside class="rounded-3xl bg-white p-5 shadow-sm"><p class="type-label text-stone-600">OUTLINE</p><button class="mt-3 w-full rounded-2xl bg-emerald-50 p-3 text-left type-label text-emerald-800">{{definition().pages[0].title}}</button><p class="mt-6 type-label text-stone-600">ADD CONTROL</p><button *ngFor="let kind of ['text','choice','boolean','integer']" (click)="addField(kind)" class="mt-2 w-full rounded-full border border-stone-200 px-3 py-2 text-left type-caption">+ {{kind}}</button></aside><div class="rounded-3xl bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="type-caption text-emerald-700">DRAFT · REVISION {{revision()}}</p><h2 class="type-h3">{{definition().title}}</h2></div><div class="flex gap-2"><button (click)="save()" class="rounded-full border border-stone-300 px-4 py-2 type-label">{{saving()?'Saving…':'Save draft'}}</button><button (click)="preview()" class="rounded-full bg-emerald-700 px-4 py-2 type-label text-white">Preview</button></div></div><div class="mt-6 space-y-4"><article *ngFor="let field of definition().pages[0].fields;let i=index" (click)="selected.set(i)" class="rounded-2xl border p-5" [class.border-emerald-600]="selected()===i" [class.border-stone-200]="selected()!==i"><p class="type-caption text-stone-500">{{field.type}} · {{field.id}}</p><p class="type-label">{{field.label}} <span *ngIf="field.required" class="text-rose-700">*</span></p><p *ngIf="field.visibleWhen" class="type-caption text-stone-600">Visible when {{field.visibleWhen.fieldId}} is {{field.visibleWhen.equals}}</p></article></div></div><aside class="rounded-3xl bg-white p-5 shadow-sm" *ngIf="definition().pages[0].fields[selected()] as field"><p class="type-label text-stone-600">PROPERTIES</p><label class="mt-4 block type-caption">Label<input [(ngModel)]="field.label" (ngModelChange)="touch()" class="mt-1 w-full rounded-xl border border-stone-300 px-3 py-2"/></label><label class="mt-4 flex items-center gap-2 type-caption"><input type="checkbox" [(ngModel)]="field.required" (ngModelChange)="touch()"/> Required</label><p class="mt-5 type-caption text-stone-600">Stable IDs are retained through label edits and moves. Rules are evaluated from the canonical definition.</p></aside></section>
<section *ngIf="view()==='preview'" class="mx-auto max-w-2xl rounded-3xl bg-white p-7 shadow-sm"><div class="mb-6 flex justify-between"><div><p class="type-caption text-emerald-700">PUBLIC FORM PREVIEW</p><h2 class="type-h3">{{definition().title}}</h2></div><span class="type-caption">Autosaved locally</span></div><p class="type-body text-stone-600">{{definition().pages[0].title}}</p><div class="mt-6 space-y-5"><ng-container *ngFor="let field of definition().pages[0].fields"><div *ngIf="visible(field)"><label class="block type-label">{{field.label}} <span *ngIf="field.required" class="text-rose-700">*</span></label><input *ngIf="field.type==='text'||field.type==='integer'" [(ngModel)]="answers[field.id]" class="mt-2 w-full rounded-xl border border-stone-300 px-4 py-3" [type]="field.type==='integer'?'number':'text'"/><select *ngIf="field.type==='choice'" [(ngModel)]="answers[field.id]" class="mt-2 w-full rounded-xl border border-stone-300 px-4 py-3"><option value="">Choose one</option><option *ngFor="let option of field.options" [value]="option.id">{{option.label}}</option></select><label *ngIf="field.type==='boolean'" class="mt-2 flex gap-2 type-body"><input type="checkbox" [(ngModel)]="answers[field.id]"/> I confirm</label><p *ngIf="errors()[field.id]" class="mt-1 type-caption text-rose-700">{{errors()[field.id]}}</p></div></ng-container></div><div class="mt-8 flex justify-between"><button (click)="view.set('builder')" class="rounded-full border border-stone-300 px-4 py-2 type-label">Back to builder</button><button (click)="submitPreview()" class="rounded-full bg-emerald-700 px-5 py-2 type-label text-white">Review & submit</button></div><div *ngIf="receipt()" class="mt-5 rounded-2xl bg-lime-50 p-4 type-body text-lime-950">{{receipt()}}</div></section>
<section *ngIf="view()==='responses'||view()==='admin'" class="space-y-5"><div class="rounded-3xl bg-white p-6 shadow-sm"><p class="type-caption text-emerald-700">RESPONSE ADMINISTRATION</p><h2 class="type-h3">Submitted responses</h2><p class="type-body text-stone-600">Response viewers inspect immutable envelopes. Exporters can download a CSV snapshot.</p><button (click)="downloadCsv()" class="mt-4 rounded-full bg-emerald-700 px-4 py-2 type-label text-white">Export CSV</button></div><div class="rounded-3xl bg-white p-6 shadow-sm"><div *ngIf="submissions().length;else noResponses"><article *ngFor="let s of submissions()" class="border-b border-stone-100 py-4"><p class="type-label">Submission {{s.id}}</p><p class="type-caption text-stone-600">{{s.submittedAt}}</p></article></div><ng-template #noResponses><p class="type-body text-stone-600">No submitted responses yet. Use Preview to complete the vertical flow.</p></ng-template></div><div *ngIf="view()==='admin'" class="rounded-3xl bg-white p-6 shadow-sm"><h2 class="type-h4">People & permissions</h2><p class="type-body text-stone-600">Local development foundation: workspace role checks are documented server-side. Production onboarding uses invitations or temporary passwords with SMTP configured separately.</p><div class="mt-4 flex gap-2"><span class="rounded-full bg-emerald-50 px-3 py-1 type-caption text-emerald-800">Owner</span><span class="rounded-full bg-sky-50 px-3 py-1 type-caption text-sky-800">Author</span><span class="rounded-full bg-lime-50 px-3 py-1 type-caption text-lime-900">Exporter</span></div></div></section>
</main></div>`,styles:[`.type-h2{font-size:clamp(2rem,4vw,3.25rem);line-height:1.1;font-weight:700}.type-h3{font-size:1.5rem;line-height:1.25;font-weight:600}.type-h4{font-size:1.25rem;font-weight:600}.type-body{font-size:1rem;line-height:1.55}.type-label{font-size:.875rem;font-weight:600;line-height:1.3}.type-caption,.type-caption-bold{font-size:.75rem;line-height:1.3}.type-caption-bold{font-weight:700}`]})
export class AppComponent { api='/v1'; nav=[{id:'forms',label:'Forms'},{id:'builder',label:'Build'},{id:'preview',label:'Preview'},{id:'responses',label:'Responses'}];view=signal('forms');forms=signal<any[]>([]);submissions=signal<any[]>([]);selected=signal(0);revision=signal(1);saving=signal(false);receipt=signal('');answers:Record<string,any>={};definition=signal<FormDef>({contractVersion:'4.0.0',formKey:'client-intake',title:'Client intake',pages:[{id:'page-intake',title:'Let’s get started',fields:[{id:'full-name',type:'text',label:'Your full name',required:true},{id:'contact-method',type:'choice',label:'How should we contact you?',required:true,options:[{id:'email',label:'Email'},{id:'phone',label:'Phone'}]},{id:'email-address',type:'text',label:'Email address',required:true,visibleWhen:{fieldId:'contact-method',equals:'email'}}]}]});errors=computed(()=>{let e:Record<string,string>={};for(const f of this.definition().pages[0].fields)if(this.visible(f)&&f.required&&!this.answers[f.id])e[f.id]='This field is required.';return e});staffToken=localStorage.getItem('smartintake.staffSession')||''; respondentId=localStorage.getItem('smartintake.respondentId')||''; respondentToken=localStorage.getItem('smartintake.respondentToken')||''; respondentRevision=0; constructor(private http:HttpClient){this.ensureStaffSession()} ensureStaffSession(){if(this.staffToken){this.loadForms();this.loadSubmissions();return}this.http.post<any>(this.api+'/auth/bootstrap',{email:'owner@local.test',password:'LocalDevelopmentPassword!'}).subscribe({next:x=>{this.staffToken=x.staffSession;localStorage.setItem('smartintake.staffSession',this.staffToken);this.loadForms();this.loadSubmissions()},error:()=>console.warn('Bootstrap is already closed; sign-in screen is required.')})} staff(){return {headers:new HttpHeaders({'X-Staff-Session':this.staffToken})}}loadForms(){this.http.get<any[]>(this.api+'/workspaces/local/forms',this.staff()).subscribe({next:x=>this.forms.set(x),error:()=>this.forms.set([])})}loadSubmissions(){this.http.get<any[]>(this.api+'/workspaces/local/submissions',this.staff()).subscribe({next:x=>this.submissions.set(x),error:()=>this.submissions.set([])})}open(f:any){this.definition.set({...this.definition(),formKey:f.formKey,title:f.title});this.revision.set(f.revision);this.view.set('builder')}touch(){this.revision.update(x=>x+1)}addField(type:string){let d=this.definition();let fields=[...d.pages[0].fields,{id:`field-${Date.now()}`,type,label:'New question',required:false}];this.definition.set({...d,pages:[{...d.pages[0],fields}]});this.selected.set(fields.length-1);this.touch()}save(){this.saving.set(true);let existing=this.forms().find(x=>x.formKey===this.definition().formKey);if(!existing){this.http.post<any>(this.api+'/workspaces/local/forms',{formKey:this.definition().formKey,title:this.definition().title},this.staff()).subscribe({next:x=>{this.revision.set(x.revision);this.saving.set(false);this.loadForms()},error:()=>this.saving.set(false)})}else{this.saving.set(false)}}preview(){this.save();let f=this.forms().find(x=>x.formKey===this.definition().formKey);if(f&&!this.respondentId)this.http.post<any>(this.api+'/public/forms/'+f.id+'/sessions',{}).subscribe(x=>{this.respondentId=x.sessionId;this.respondentToken=x.respondentSession;this.respondentRevision=x.revision;localStorage.setItem('smartintake.respondentId',x.sessionId);localStorage.setItem('smartintake.respondentToken',x.respondentSession)});this.view.set('preview')}visible(field:Field){return !field.visibleWhen||this.answers[field.visibleWhen.fieldId]===field.visibleWhen.equals}submitPreview(){if(Object.keys(this.errors()).length){this.receipt.set('Please correct the highlighted fields before submitting.');return}if(!this.respondentId){this.receipt.set('Publish the form before starting a respondent session.');return}let h={headers:new HttpHeaders({'X-Respondent-Session':this.respondentToken})};this.http.patch<any>(this.api+'/sessions/'+this.respondentId,{baseRevision:this.respondentRevision,answers:this.answers},h).subscribe({next:x=>{this.respondentRevision=x.acceptedRevision;this.http.post<any>(this.api+'/sessions/'+this.respondentId+'/submissions',{sessionRevision:this.respondentRevision},h).subscribe({next:r=>{this.receipt.set('Response received. Receipt '+r.receiptId);localStorage.removeItem('smartintake.respondentId');localStorage.removeItem('smartintake.respondentToken');this.view.set('responses')},error:()=>this.receipt.set('Server validation blocked submission.')})},error:()=>this.receipt.set('Autosave conflict. Reload the session.')})}downloadCsv(){window.open(this.api+'/workspaces/local/exports.csv','_blank')}}
