# Smart Form Builder Lite v1.1 evaluator corpus

This tree is the independent evaluator's expected-outcome corpus, version 1.0.0. It is derived solely from the PRD and handoff, not from current product output. `manifest.json` is the normative inventory and status record. Measured conformance has not run; package wire instances, native Hindi/Arabic review, sealed sentinel payloads, and the authoritative seven-ID denial list remain blocked where stated.
