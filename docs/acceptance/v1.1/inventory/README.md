# M0 authoritative inventory

Generated from the repository PRD/handoff by `tools/acceptance/generate_inventory.py`. Run `python3 tools/acceptance/check_m0.py` for the overall strict M0 gate; `python3 tools/acceptance/generate_inventory.py --check` verifies this inventory surface only. The current handoff has no explicitly labelled authoritative denial-list addendum, so the overall strict M0 gate remains blocked with `M0-BLOCKER: denial-list-unenumerated` rather than scanning arbitrary prose for IDs. Use `--check --allow-unenumerated-denial-list` only to reproduce the remaining inventory while the authoritative source is corrected. Run `--self-check` for focused negative anchor, path, style traversal, and denial-source tests. Operation records use `method` and canonical `path`; O_named remains 53 explicit table operations and its separately cited named-supporting inventory totals 55. This is provenance and denominator material only; it assigns no product acceptance status.
